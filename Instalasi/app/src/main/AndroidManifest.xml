<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.instalasi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".tentang"></activity>
        <activity android:name=".webviewlinux" />
        <activity android:name=".webview10" />
        <activity android:name=".webview8" />
        <activity android:name=".webview7" />
        <activity android:name=".windows10" />
        <activity android:name=".windows8" />
        <activity android:name=".windows7" />
        <activity android:name=".windows" />
        <activity android:name=".Linux" />
        <activity android:name=".aksilanjut" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>